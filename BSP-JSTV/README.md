# BSP-WS
I put this together in pretty much a single night. Don't expect much.

The mod listens for messages at the address `ws://127.0.0.1:9060/sock`.

It is expected that messages are JSON like this `{"username":"milkydelta", "msg":"!bsr 461af"}`, sent in text mode.

No messages are ever sent back to you.
